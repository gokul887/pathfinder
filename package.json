{
  "homepage": "https://gokul887.github.io/pathfinder",
  "devDependencies": {
    "gh-pages": "^6.0.0"
  },
  "scripts": {
    "predeploy": "cd client && npm run build && cd ..",
    "deploy": "cd client && gh-pages -d build && cd .."
  }
}
